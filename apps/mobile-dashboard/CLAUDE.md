# Mobile Dashboard Agent
You own ONLY apps/mobile-dashboard/.
Depends on: api-gateway endpoint contracts.
Stack: React Native + Expo, Zustand, React Query, Axios.
NEVER edit: packages/*, other apps/*, .env*, docker-compose.yml.
Run `npm test` before completing any task.
