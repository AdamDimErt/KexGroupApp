# API Gateway Agent
You own ONLY apps/api-gateway/src/.
Schema: packages/database/schema.prisma (READ ONLY).
Depends on: auth-service JWT contracts (payload format, guard decorator).
NEVER edit: packages/*, other apps/*, .env*, docker-compose.yml.
Run `npm test` before completing any task.
