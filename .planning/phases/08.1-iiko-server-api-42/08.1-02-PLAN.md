---
phase: 08.1-iiko-server-api-42
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile-dashboard/src/screens/PointDetailScreen.tsx
  - apps/mobile-dashboard/src/screens/PointDetailScreen.styles.ts
  - apps/mobile-dashboard/src/screens/ReportsScreen.tsx
  - apps/mobile-dashboard/src/screens/ReportsScreen.styles.ts
autonomous: false
requirements:
  - DDS-01
  - DDS-02

must_haves:
  truths:
    - "PointDetailScreen shows ALL DDS expense groups with color-coded icons, not just Продукты and Прочие"
    - "ReportsScreen DDS summary shows per-restaurant breakdown when expanded"
    - "Expense groups have recognizable color-coded left border or icon per group type"
    - "Empty expense groups are hidden (only groups with data shown)"
  artifacts:
    - path: "apps/mobile-dashboard/src/screens/PointDetailScreen.tsx"
      provides: "Enhanced expense groups section with icons and colors"
      contains: "GROUP_COLORS"
    - path: "apps/mobile-dashboard/src/screens/ReportsScreen.tsx"
      provides: "DDS summary with expandable per-restaurant rows"
      contains: "expandedGroup"
  key_links:
    - from: "apps/mobile-dashboard/src/screens/PointDetailScreen.tsx"
      to: "apps/mobile-dashboard/src/hooks/usePointDetail.ts"
      via: "expenseGroups from usePointDetail hook"
      pattern: "expenseGroups"
    - from: "apps/mobile-dashboard/src/screens/ReportsScreen.tsx"
      to: "apps/mobile-dashboard/src/hooks/useReports.ts"
      via: "dds.data.groups from useReportDds"
      pattern: "dds\\.data"
---

<objective>
Enhance mobile screens to beautifully display all DDS expense categories (Аренда, ЗП, Коммуналка, Комиссии банков, etc.) instead of the current 2-group view, and make the DDS summary report interactive with per-restaurant drill-down.

Purpose: When real DDS data flows from iiko Server API sync (Plan 01), the mobile screens must be ready to display 12 expense groups beautifully and meaningfully. The current UI only shows "Продукты питания" and "Прочие расходы" — it needs to show all group categories with visual distinction.

Output: Enhanced PointDetailScreen expense section + interactive ReportsScreen DDS summary.
</objective>

<execution_context>
@D:/kexgroupapp/.claude/get-shit-done/workflows/execute-plan.md
@D:/kexgroupapp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types already defined in mobile — no new types needed -->

From apps/mobile-dashboard/src/types/index.ts:
```typescript
export interface ExpenseGroupDto {
  groupId: string;
  groupName: string;
  totalAmount: number;
  articleCount: number;
}

export interface ReportDdsItemDto {
  groupName: string;
  groupId: string;
  totalAmount: number;
  restaurants: { restaurantId: string; restaurantName: string; amount: number }[];
}

export interface ReportDdsDto {
  period: PeriodDto;
  groups: ReportDdsItemDto[];
  grandTotal: number;
}
```

From packages/database/seed.ts — 12 DdsArticleGroup codes and Russian names:
- FOOD: Продукты питания
- RENT: Аренда помещений
- SALARY: Заработная плата
- UTILITIES: Коммунальные услуги
- MARKETING: Маркетинг и реклама
- IT: IT и связь
- TRANSPORT: Транспорт и доставка
- EQUIPMENT: Оборудование и ремонт
- TAXES: Налоги и сборы
- OTHER: Прочие расходы
- BANK_FEE: Комиссии банков
- KITCHEN: Цех (производство)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance PointDetailScreen expense groups with color-coded categories</name>
  <files>
    apps/mobile-dashboard/src/screens/PointDetailScreen.tsx
    apps/mobile-dashboard/src/screens/PointDetailScreen.styles.ts
  </files>
  <read_first>
    apps/mobile-dashboard/src/screens/PointDetailScreen.tsx
    apps/mobile-dashboard/src/screens/PointDetailScreen.styles.ts
    apps/mobile-dashboard/src/hooks/usePointDetail.ts
    apps/mobile-dashboard/src/types/index.ts
  </read_first>
  <action>
    The current "Расходы по группам" section shows expense groups as plain rows with a single-color bar. Enhance it to:

    1. **Add GROUP_COLORS and GROUP_ICONS constants** at the top of the file (before the component):
       ```typescript
       const GROUP_COLORS: Record<string, string> = {
         'Продукты питания': '#10B981',     // green
         'Аренда помещений': '#6366F1',     // indigo
         'Заработная плата': '#F59E0B',     // amber
         'Коммунальные услуги': '#06B6D4',  // cyan
         'Маркетинг и реклама': '#EC4899',  // pink
         'IT и связь': '#8B5CF6',           // violet
         'Транспорт и доставка': '#F97316', // orange
         'Оборудование и ремонт': '#64748B',// slate
         'Налоги и сборы': '#EF4444',       // red
         'Прочие расходы': '#94A3B8',       // gray
         'Комиссии банков': '#3B82F6',      // blue
         'Цех (производство)': '#14B8A6',   // teal
       };
       const GROUP_EMOJI: Record<string, string> = {
         'Продукты питания': '🍔',
         'Аренда помещений': '🏠',
         'Заработная плата': '👥',
         'Коммунальные услуги': '💡',
         'Маркетинг и реклама': '📣',
         'IT и связь': '💻',
         'Транспорт и доставка': '🚛',
         'Оборудование и ремонт': '🔧',
         'Налоги и сборы': '📋',
         'Прочие расходы': '📦',
         'Комиссии банков': '🏦',
         'Цех (производство)': '🏭',
       };
       ```
       NOTE: The user specifically asked for emoji icons ("красиво") — this is the exception to the no-emoji rule.

    2. **Redesign each expense group row** in the "Расходы по группам" section:
       - Left: 4px colored left border (color from GROUP_COLORS by groupName, fallback #94A3B8)
       - Emoji icon (from GROUP_EMOJI by groupName, fallback '📦')
       - Group name
       - Right: amount formatted with fmtAmount + percentage of total expenses
       - Below the amount: horizontal progress bar with group color
       - If canDrillToLevel3: chevron '›' on the right
       - Keep existing TouchableOpacity + onNavigateArticle for drill-down

    3. **Add expense summary header** above the groups:
       - "Расходы по группам" title (existing)
       - Add subtitle: "Всего: ₸{total}" where total = sum of all group amounts
       - Add article count: "{N} категорий"

    4. **Sort groups by totalAmount descending** so biggest expenses appear first.

    5. **Add styles** to PointDetailScreen.styles.ts:
       - expGroupRow: flexDirection row, padding 12, borderLeftWidth 4, borderRadius 8, marginBottom 8, backgroundColor rgba(255,255,255,0.05)
       - expGroupIcon: fontSize 18, marginRight 8
       - expGroupInfo: flex 1
       - expGroupName: color white, fontSize 14, fontWeight 600
       - expGroupBar: height 3, borderRadius 2, marginTop 6
       - expGroupRight: alignItems flex-end
       - expGroupAmount: color white, fontSize 14, fontWeight 700
       - expGroupPct: color rgba(255,255,255,0.5), fontSize 11

    Keep ALL existing functionality (revenue by payment types, financial result, cash discrepancies, daily chart). Only modify the "Расходы по группам" section.
  </action>
  <verify>
    <automated>cd apps/mobile-dashboard && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - GROUP_COLORS constant maps 12 group names to distinct colors
    - GROUP_EMOJI constant maps 12 group names to emoji icons
    - Expense groups sorted by totalAmount descending
    - Each group row has colored left border, emoji, name, amount, percentage, and progress bar
    - Summary line shows total expenses and category count
    - Existing drill-down navigation (onNavigateArticle) preserved
    - tsc --noEmit passes
  </acceptance_criteria>
  <done>PointDetailScreen expense groups section redesigned with color-coded categories, emoji icons, sorted by amount, percentage display, tsc clean</done>
</task>

<task type="auto">
  <name>Task 2: Enhance ReportsScreen DDS summary with expandable per-restaurant breakdown</name>
  <files>
    apps/mobile-dashboard/src/screens/ReportsScreen.tsx
    apps/mobile-dashboard/src/screens/ReportsScreen.styles.ts
  </files>
  <read_first>
    apps/mobile-dashboard/src/screens/ReportsScreen.tsx
    apps/mobile-dashboard/src/screens/ReportsScreen.styles.ts
    apps/mobile-dashboard/src/hooks/useReports.ts
    apps/mobile-dashboard/src/types/index.ts (ReportDdsDto, ReportDdsItemDto)
  </read_first>
  <action>
    The current "ДДС сводный" section shows flat group rows. Enhance it to show expandable per-restaurant breakdown and reuse the same color scheme from PointDetailScreen.

    1. **Add same GROUP_COLORS constant** at the top (copy from Task 1 — or extract to a shared constants file if cleaner, but given mobile scope, inline is fine).

    2. **Add expandable state:**
       ```typescript
       const [expandedGroup, setExpandedGroup] = useState<string | null>(null);
       ```

    3. **Redesign each DDS group row** to be tappable:
       - Left: 4px colored border (GROUP_COLORS by groupName)
       - Group name
       - Right: total amount + expand chevron ('▸' collapsed, '▾' expanded)
       - On tap: toggle expandedGroup state

    4. **When expanded**, show per-restaurant breakdown below the group row:
       - Each restaurant as an indented sub-row:
         - Restaurant name (left)
         - Amount (right)
         - Thin colored bar proportional to restaurant amount / group total
       - The restaurants array is already in ReportDdsItemDto.restaurants
       - Sort restaurants by amount descending

    5. **Add percentage column** to each group: `(groupTotal / grandTotal * 100).toFixed(1)%`

    6. **Visual improvements:**
       - Grand total displayed prominently at top with large font
       - "ДДС сводный" title + "по всем точкам" subtitle
       - Group rows with subtle background (rgba(255,255,255,0.05))
       - Restaurant sub-rows with lighter background (rgba(255,255,255,0.03))
       - Smooth height animation is NOT required (simple show/hide is fine for performance)

    7. **Add styles** to ReportsScreen.styles.ts:
       - ddsGroupRow: flexDirection row, padding 12, borderLeftWidth 4, borderRadius 8, marginBottom 4, backgroundColor rgba(255,255,255,0.05)
       - ddsRestaurantRow: flexDirection row, paddingVertical 6, paddingHorizontal 16, marginLeft 16
       - ddsChevron: fontSize 12, color rgba(255,255,255,0.4), marginLeft 8
       - ddsPct: color rgba(255,255,255,0.5), fontSize 11, marginLeft 4

    Keep ALL existing report sections (Company Expenses, Kitchen, Trends) unchanged.
  </action>
  <verify>
    <automated>cd apps/mobile-dashboard && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - expandedGroup useState exists in ReportsScreen
    - DDS group rows are tappable with expand/collapse chevron
    - Expanded group shows per-restaurant breakdown sorted by amount desc
    - Each group row has colored left border matching GROUP_COLORS
    - Percentage column shows group share of grand total
    - Grand total displayed prominently
    - All other report sections (Company, Kitchen, Trends) unchanged
    - tsc --noEmit passes
  </acceptance_criteria>
  <done>ReportsScreen DDS summary enhanced with expandable per-restaurant breakdown, color-coded groups, percentage display, tsc clean</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of enhanced DDS screens</name>
  <files>apps/mobile-dashboard/src/screens/PointDetailScreen.tsx, apps/mobile-dashboard/src/screens/ReportsScreen.tsx</files>
  <action>User verifies the visual design of enhanced DDS expense screens in Expo Go or simulator.</action>
  <verify>
    1. Run `cd apps/mobile-dashboard && npx expo start` or view in Expo Go
    2. Navigate to any restaurant (PointDetailScreen):
       - Verify "Расходы по группам" shows colored categories with emoji icons
       - Verify groups sorted by amount (biggest first)
       - Verify percentage of total shown for each group
       - Tap a group to verify drill-down to ArticleDetailScreen still works
    3. Navigate to Reports tab (ReportsScreen):
       - Verify "ДДС сводный" shows colored group rows
       - Tap a group to expand and see per-restaurant breakdown
       - Verify restaurants sorted by amount within expanded group
       - Verify other sections (Цех, Тренды) unchanged
    4. Verify no visual regressions on other parts of the screens
  </verify>
  <done>User approves visual design of both enhanced screens</done>
</task>

</tasks>

<verification>
- `cd apps/mobile-dashboard && npx tsc --noEmit` — no type errors
- PointDetailScreen renders all expense groups with colors and icons
- ReportsScreen DDS summary is expandable with per-restaurant data
- No regressions in other screen sections
</verification>

<success_criteria>
- PointDetailScreen shows 12 color-coded expense group categories (when data exists)
- ReportsScreen DDS summary has expandable per-restaurant drill-down
- Both screens compile without TypeScript errors
- User approves visual design at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-iiko-server-api-42/08.1-02-SUMMARY.md`
</output>
