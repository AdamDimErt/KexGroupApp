# UI Audit — Mobile Dashboard — 2026-04-20

## Summary
- **Overall: 32/60**
- **Verdict: NEEDS-REWORK** (contract exists, but screens bypass it)
- **Files reviewed:** 11 screens, 15 components, 9 theme modules

Token library (`src/theme/*`) is **gold-standard v3-compliant**. Component library (`src/components/HeroCard.tsx`, `KPICard.tsx`, `KPIRow.tsx`, `Button.tsx`, `Chip.tsx`, `Badge.tsx`, `Chart.tsx`, `Input.tsx`) is **architecturally correct** — variants, semantic border-left, pulse dots, tabular Fira Code, state machines. **Screens do not consume any of these components.** Grep for `<HeroCard|<KPICard|<KPIRow|<Button|<Chip|<Badge`: **0 matches** across 11 screens. The DashboardScreen reinvents HeroCard inline (`DashboardScreen.styles.ts:38-52`) with wrong visual (solid blue bg instead of bgCard + border-left), wrong typography (no Fira Code), and wrong tokens (hardcoded `#10B981`, `rgba(255,255,255,0.6)`).

## Scores
| Pillar | Score | Grade |
|---|---|---|
| Visual Hierarchy | 6/10 | C |
| Typography | 4/10 | D |
| Spacing | 5/10 | C |
| Color | 5/10 | C |
| Components | 3/10 | D |
| Accessibility | 9/10 | A |

## Critical issues (must-fix for demo)

- **`DashboardScreen.tsx:150-185`** — The 3 KPI cards are inline JSX, not `<KPIRow>` component. Missing: semantic border-left (spec §3.3), delta %, period label, Fira Code typography. Fix: replace block with `<KPIRow revenue={…} expenses={…} balance={…} period={…} />`. Component exists at `components/KPIRow.tsx` and is unused.

- **`DashboardScreen.styles.ts:38-52`** — Hero card is solid blue gradient (`backgroundColor: colors.accentDefault`, `fontSize: 36`, weight `500`). Spec §3.5 requires `bg #0F172A`, `borderLeft 4px`, `radius 16`, value in Fira Code 36/700 tabular. Fix: `<HeroCard variant="primary" label="ВЫРУЧКА" value={totalRevenue} ... />` — component already implemented at `components/HeroCard.tsx`.

- **`DashboardScreen.tsx:180`, `RevenueDetailScreen.tsx:43`, `PointDetailScreen.tsx:58,108,165-168,298,333,351`, `ArticleDetailScreen.tsx:89`, `OperationsScreen.tsx:95`, `ReportsScreen.tsx:117,163,166`** — Hardcoded hex `#10B981` (wrong green — spec token is `#22C55E`), `#EF4444`, `#F59E0B`, `#3B82F6`, `#6366F1`. Antipattern per Rules §6 («✗ Hardcoded `#2563EB` in components — use `theme.colors.accent`»). Fix: replace with `colors.status.positive / danger / warning` tokens. 57 occurrences across 14 files.

## High-priority (fix before prod)

- **`DashboardScreen.styles.ts:9,54,60` · `LoginScreen.styles.ts:12,14,26,30,31,34,42,47,63,66,75,78,80,81,83,92,97` · `BrandDetailScreen.styles.ts:9` · `PointsScreen.styles.ts:6` · `RevenueDetailScreen.styles.ts:19`** — `paddingTop: 60`, `marginTop: 80`, `marginTop: 52`, `padding: 14`, `width: 52, height: 52`, `borderRadius: 14`, `fontSize: 13/11/10/9`, `gap: 6/10`, `marginBottom: 14`, `top: 2 right: 2` etc. Rules §1.1 forbids non-4px-grid values (`5/6/7/10/11/13/14/18`). 116 violations on font-size alone. Fix: snap to `spacing.xs2/xs/sm/md/lg/xl` and `headings/body` tokens.

- **`DashboardScreen.styles.ts:38-52`** — Hero uses `borderRadius: 20` (not in spec scale 4/8/12/16/20 — 20 is allowed but inappropriate for hero; spec §3.5 says `radius 16`). `letterSpacing: -1.5` (spec max is `-0.5` for `display-numeric`). Inline `shadowRadius: 28` (outside shadow tokens — spec §1.12 has only `sm 6 / md 16 / lg 16 / xl 32 / 2xl 48`).

- **`DashboardScreen.styles.ts:45,47,48,51,52,89,119` · `DashboardScreen.tsx:190` · `LoginScreen.styles.ts:37,39,45,50,58,82,108,113`** — Text opacity via `rgba(255,255,255,0.5/0.6/0.55/0.35/0.12)` instead of solid hex text hierarchy tokens. Rules §1.3 is categorical: «Антипаттерн: opacity вместо solid hex на Text». Fix: use `colors.text.muted/tertiary/disabled`, pill bg `rgba()` only via `colors.pill.*`.

- **`DashboardScreen.tsx:61`** — Hardcoded brand string "Kex Group" is acceptable, but `LoginScreen.tsx:34` "HoldingView" + subtitle "Весь холдинг — одним взглядом" is off-brand for KEX GROUP. Rules §4.3 bans fictional names. Confirm with PO whether this is the intended product name.

- **Typography family neglect** — Only `RevenueDetailScreen.styles.ts` references a `fontFamily` (7 times), and none are Fira Code. All numeric-heavy screens (Dashboard hero `fontSize: 36`, KPI values, payment amounts, chart labels) render as default system sans. Rules §1.7: Fira Code mandatory on `displayNumeric`, deltas, amounts, timestamps. Fix: every `<Text>` wrapping a number must get `...typography.displayNumeric` or `fontFamily: fontFamilies.monoBold`.

- **`DashboardScreen.tsx:164`** — KPI value uses `formatAmount()` → `₸8.2M`. Fira Code tabular-nums not applied. Spec §4.6 requires `tabular-nums` + Cyrillic "М" marker (not Latin "M"). Fix: use `formatKpiValue` from `KPICard.tsx:33` (already uses "М"/"К" correctly).

## Low-priority (nice-to-have)

- **`LoginScreen.styles.ts:65-77`** — Login CTA uses `borderRadius: 14` + `padding: 16` instead of spec pill `radius: 9999, height: 52` for login full-width (Rules §3.1 «Pill for login»). Component `Button.tsx` with `radius="pill"` variant exists — use it.

- **`LoginScreen.styles.ts:79-85`** — OTP cells `height: 58`, `borderRadius: 14`, `fontSize: 20`, `gap: 6`. Spec §3.2: cells `48×48`, `radius 12`, `gap 8`. Component `OtpInput.tsx` exists but unused.

- **`LoginScreen.styles.ts:49-52`** — Phone prefix divider `backgroundColor: 'rgba(59,130,246,0.18)'`. Spec §3.2: `1px #334155`. Current tint is accent-hued instead of neutral.

- **`DashboardScreen.tsx:205-209`** — Source badges "iiko"/"1С" use inline tint (`rgba(255,255,255,0.12)`) inside the solid-blue hero. Spec §3.5 + `chips-badges.html:371`: source badges must be solid light (`bg #F8FAFC`, text `#0F172A`). Token `colors.srcBadgeBg` / `srcBadgeText` is already defined and unused.

- **`DashboardScreen.tsx:131-142`** — Bell icon + Settings + LogOut buttons have `padding: 4/6`. Rules §5.1 requires 44pt min touch target. Currently ~24×24pt. Fix: add `hitSlop={{ top: 12, right: 12, bottom: 12, left: 12 }}` or bump padding to 12.

- **`DashboardScreen.tsx:215-220` · `RevenueDetailScreen.tsx:401`** — Green "↑ Результат" / "Выручка" rows use `colors.sparkGreen` (`#10B981`, legacy field restored in `colors.ts:211`) or `colors.green` alias. Migrate to `colors.status.positive` (`#22C55E`).

- **`RevenueDetailScreen.tsx:43-52`** — `KNOWN_PAYMENT_COLORS` reinvents a palette (Cash `#10B981`, Kaspi `#F59E0B`, …). Spec has 12 DDS category tokens in `theme/categories.ts` — payment types aren't DDS categories but the same governance applies: centralize the mapping in `theme/payments.ts`. Currently duplicated in `PointDetailScreen.tsx:165-168` too.

- **`DashboardScreen.styles.ts:20`** — Bell badge `width: 8, height: 8, borderRadius: 4, borderWidth: 1.5`. `borderWidth: 1.5` not in grid; use `1` or `2`.

- **`RevenueDetailScreen.tsx:179`** — Selected bar color = `colors.green` (status.positive). Spec §3.6 says active bar = `accent-light #60A5FA`; positive green is semantic, not active. Swap to `colors.chart.barSelected`.

- **`RevenueDetailScreen.tsx:67-77`** — Bar width computed from `Dimensions.get('window').width - 80` with magic `MAX_BAR_W = 48`, `- 6` gap. Chart component exists at `components/Chart.tsx` — should be consumed.

- **`DashboardScreen.tsx:93-104`** — Error retry uses inline `TouchableOpacity` with `#FFF` text — spec button tokens missing. Reuse `<Button variant="primary" size="md" loading={…} />` from `components/Button.tsx`.

## What's done well

- **Accessibility: strong.** All interactive elements (`DashboardScreen.tsx:159-161,249-252`, `RevenueDetailScreen.tsx:248-250,286,294,336,487`) have `accessibilityRole="button"` + `accessibilityLabel`. 14 hits across Dashboard + RevenueDetail. OTP screen uses `selectTextOnFocus`. RefreshControl on every list.
- **Token library v3.** `theme/colors.ts`, `spacing.ts`, `typography.ts`, `semantic.ts`, `categories.ts`, `radii.ts` are an exact materialization of the 17-HTML prototype spec, with deprecation aliases for gradual Wave 5 migration. This is a strong foundation.
- **Component library quality.** `HeroCard.tsx`, `KPICard.tsx`, `KPIRow.tsx`, `RestaurantCard.tsx`, `Chart.tsx`, `OfflineBanner.tsx`, `SkeletonLoader.tsx` are production-grade — variants, skeleton/empty/error states, pulse animation, semantic border-left, tabular Fira Code, proper token consumption.
- **`RestaurantCard.styles.ts`** — exemplary. Radius 12, border-left dynamic, paddings 16/18, brand badges from `colors.brand`, tabular Fira Code on revenue (`:98`), 9px `planLabel` in Fira Code SemiBold (`:153-157`). Zero hex literals. Copy this pattern to the rest.
- **Loading + error + offline states.** `DashboardScreen.tsx:55-105` has full skeleton, error-with-retry, offline banner. `RevenueDetailScreen.tsx:242-302` same. States are first-class.
- **Brand compliance.** No "Kex Pizza", "AliPush", "fast food", no "Пицца" cuisine. Restaurants come from iiko data (real).
- **Semantic pill palette.** `colors.pill.{positive|warning|danger|info|accent}` used correctly in `RevenueDetailScreen.tsx:432-445` (only place screens consume nested tokens). Gold template.

---

**Verdict rationale:** Token and component layers are contract-compliant; the screens layer is a pre-v3 implementation that shadows the new primitives instead of consuming them. This is a mechanical migration (swap inline JSX for `<HeroCard>`, `<KPIRow>`, `<Button>`), not a redesign — but until it happens the dashboard won't read as v3. Estimated effort: 2-3 days for all 11 screens. Priority order: Dashboard (demo face) → Login (first impression) → RevenueDetail → rest.
